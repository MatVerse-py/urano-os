# Spatial OSX feature branch

Review target: `master`.

This branch is intentionally contract-first. It adds the minimum stable boundaries needed before importing the larger web/XR prototype into the canonical runtime.
