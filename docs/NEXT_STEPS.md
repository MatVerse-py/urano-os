# Next steps

1. Bind the current web prototype to `ResearchState` and `SpatialSceneSpec` instead of local UI state.
2. Replace hard-coded provider/device status with health adapters.
3. Require SOVEREIGN = LOCAL-only with no provider exceptions.
4. Convert Model Trace into records derived from real `ModelRun` receipts.
5. Keep PAPER and SPACE as renderer modes over the same investigation state.
6. Add tests proving deterministic scene generation from identical state+seed.
7. Add platform adapters only after renderer boundary tests pass.
8. Validate visionOS/OpenXR on actual SDK/runtime/hardware before promotion beyond HOLD.
